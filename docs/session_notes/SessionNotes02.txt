SESSION NOTES — HMS (Hygiene Management System)
DATE: 2026‑04‑11
SESSION SCOPE: Phase 4 completion → Phase 7A (Employee CRUD) → Postman auth and automation → Document schema correction → Document domain foundations (entities, FK, repositories, service)
SESSION RESULT: Employee master‑data complete and demo‑ready; Document domain fully modeled at entity + repository + service level, ready for API exposure and future extensions.

CONTEXT SNAPSHOT
Application: HMS (Hygiene Dept Management System)
Tech Stack: Spring Boot, Spring Security (session‑based), Spring Data JPA, MySQL 8
Architecture: Database‑first, Hibernate used only for mapping, manual schema control
Security: Form login, server‑side HTTP session (JSESSIONID cookie)
API Style: REST, JSON, /api/** endpoints secured
Timezone Strategy: UTC enforced across app and DB access
Testing Strategy: Manual API testing via Postman (no automated tests yet)

PHASE 4 — DATABASE SCHEMA (FINALIZED & CORRECTED)
Decisions:

Flyway intentionally skipped for MVP
Schema managed via manual SQL committed to repository
Main schema file: schema_v1.sql
Sample data file: data_initialisation_v1.sql
ENGINE explicitly set to InnoDB for all tables
Foreign keys defined inline during CREATE TABLE
FOREIGN_KEY_CHECKS disabled and re‑enabled during schema reset
Session‑level UTC timezone enforced, not GLOBAL
SET GLOBAL time_zone deliberately removed from schema
Database explicitly selected in schema script using USE hygiene_app;
Schema execution verified via IntelliJ Database Tool (CLI execution unreliable without explicit DB selection)

Schema characteristics:

BIGINT AUTO_INCREMENT primary keys
Soft delete via active BOOLEAN flag
Immutable tables:

TrainingInstance
DocumentVersion
AuditLog


Join tables for many‑to‑many:

training_trainee
training_document



DocumentVersion schema correction (important):

Identified missing business‑date column in DocumentVersion
Added:

version_issue_date DATE NOT NULL


Purpose:

Distinguish when a document version was issued/effective vs when added to system
created_at retained strictly for system/audit timestamp


Java mapping:

version_issue_date → LocalDate
created_at → Instant



Verification:

Schema recreated successfully via IntelliJ Database Tool
version_issue_date column confirmed present
MySQL CLI issue traced to missing DB context
Root cause resolved by using IntelliJ execution and adding USE hygiene_app;
Sample data updated with realistic version_issue_date values
Referential integrity verified manually


PHASE 7A — EMPLOYEE MASTER DATA (COMPLETE)
Employee Entity:

@Entity + @Table(name="employee")
Fields:

id (BIGINT, identity)
employeeNumber (String, optional, unique business key)
firstName
lastName
department
jobRole
active (soft delete)
createdAt (Instant, UTC)
updatedAt (Instant, UTC)


Lifecycle callbacks:

@PrePersist → createdAt
@PreUpdate → updatedAt


No relationships defined yet (intentional)

Repository:

JpaRepository<Employee, Long>
Derived queries:

existsByEmployeeNumber
findByEmployeeNumber



Service Responsibilities:

Enforce business rules
Soft delete semantics
Uniqueness validation
Prevent updates on inactive employees
Deactivate is idempotent
No physical deletes

Exceptions:

IllegalStateException used for MVP
Domain‑specific exceptions deferred

Controller:
Base path: /api/employees
Endpoints:

POST   /api/employees
GET    /api/employees
PUT    /api/employees/{id}
PATCH  /api/employees/{id}/deactivate


SECURITY + POSTMAN AUTH (VERIFIED)
Security model:

Spring Security form login
Session‑based authentication
JSESSIONID stored in cookie
/api/** requires authentication
CSRF disabled for /api/**

Postman:

Manual POST /login required
Session cookie reused automatically
No Authorization headers required

Verification:

Authenticated access → 200
Unauthenticated access → 401


POSTMAN COLLECTION & AUTOMATION
Collection: HMS API (Local)
Folders:
Auth

POST Login

Employees

POST   Create
GET    List Active
PUT    Update by ID
PATCH  Deactivate

Collection variables:

baseUrl
employeeId
employeeNumber

Path variables:

Spring {id} replaced with {{employeeId}}

Post‑response automation:

Capture created employeeId
Auto‑increment employeeNumber for demo flow

Script:
if (pm.response.code === 201) {
const response = pm.response.json();
const match = response.employeeNumber.match(/(\D*)(\d+)/);
const prefix = match[1];
const number = parseInt(match[2], 10);
pm.collectionVariables.set("employeeId", response.id);
pm.collectionVariables.set("employeeNumber", prefix + (number + 1));
}

VERIFIED END‑TO‑END FLOWS

Create employee → 201
Duplicate employeeNumber → error
Update active employee → 200
Deactivate employee → 204
Inactive employee excluded from list
Repeated deactivate is idempotent


DOCUMENT DOMAIN — FOUNDATIONS COMPLETE
Entities:
DocumentReference:

Aggregate root
Soft‑deletable
Fields:

id
referenceCode (unique)
originDepartment
mandatory
active
createdAt (Instant)



DocumentVersion:

Immutable child entity
Fields:

id
version
versionIssueDate (LocalDate, business date)
isCurrent
defaultTrainingValidityDays (Integer, nullable)
createdAt (Instant)


FK mapping:

@ManyToOne → DocumentReference
Unidirectional
FetchType.LAZY
No cascade


Raw foreign key ID deliberately removed from entity

Relationship decisions:

Foreign key owned by DocumentVersion (correct per schema)
Unidirectional mapping chosen intentionally
No parent‑side @OneToMany added yet
Aggregate boundaries preserved
Immutability preserved

Repositories:
DocumentReferenceRepository:

findByReferenceCode
existsByReferenceCode

DocumentVersionRepository:

findByDocumentReference
findByDocumentReferenceAndIsCurrentTrue
existsByDocumentReferenceAndIsCurrentTrue
findByDocumentReferenceOrderByVersionIssueDateDesc

Service:
DocumentService implemented with:

Reference creation with uniqueness enforcement
Version creation:

Requires valid, active reference
Enforces exactly one current version
Previous current version automatically unset


Query methods:

Get all versions for a reference
Get current version for a reference


All operations transactional
Version mutability intentionally restricted
Future correction workflows discussed conceptually but deferred


CURRENT STATE & NEXT STEPS
Completed:

Phase 4 schema + corrections
Phase 7A Employee CRUD
Security + Postman auth + demo flows
DocumentReference & DocumentVersion entities
FK mapping between reference and version
Document repositories
Document service with core invariants

Explicitly not done yet:

DTOs
Validation annotations
Domain‑specific exception hierarchy
DocumentController (API)
Training module
Correction / invalidation workflow for document versions

Recommended next session:

DTOs + validation (Employees + Documents)
OR
DocumentController (REST layer)
OR
Training module groundwork (now unblocked by Documents)


SESSION END STATE
System is stable and coherent. Employee domain fully demo‑ready. Document domain modeled correctly at schema, entity, repository, and service layers, with compliance‑grade invariants enforced. Architecture prepared for API exposure and future correction workflows without rework.